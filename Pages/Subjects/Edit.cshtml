@page
@model AdminEmentor.Pages.Subjects.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Subject</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Subject.SubjectID" />
            <div class="form-group">
                <label asp-for="Subject.SubjectID" class="control-label"></label>
                <div>@Html.DisplayFor(model => model.Subject.SubjectID)</div>
            </div>
            <div class="form-group">
                <label asp-for="Subject.Title" class="control-label"></label>
                <input asp-for="Subject.Title" class="form-control" />
                <span asp-validation-for="Subject.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Subject.Credits" class="control-label"></label>
                <input asp-for="Subject.Credits" class="form-control" />
                <span asp-validation-for="Subject.Credits" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Subject.Department" class="control-label"></label>
                <select asp-for="Subject.DepartmentID" class="form-control" asp-items="@Model.DepartmentNameSL"></select>
                <span asp-validation-for="Subject.DepartmentID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
